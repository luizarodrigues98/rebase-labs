#!/bin/bash

docker run \
  --rm \
  --name rebase-labs \
  -w /rebase_labs \
  -v $(pwd):/rebase_labs \
  -v rubygems_rebase_labs:/usr/local/bundle \
  -p 3000:3000 \
  --network rebase_labs \
  ruby \
  bash -c "bundle install && rake db:reset"